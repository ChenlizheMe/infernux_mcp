# Infernux MCP

`infernux.core.mcp` is the default MCP integration plugin for the Infernux
editor.  This repository currently contains the minimal installable plugin
shell; the existing engine MCP implementation will be migrated here in later
iterations.

The plugin is editor-only by default and is therefore excluded from Player
builds.
